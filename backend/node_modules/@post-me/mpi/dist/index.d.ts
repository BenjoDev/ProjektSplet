/**
 * @packageDocumentation MPI-like concurrency for a pool of web workers
 */
import { createPool, WorkerPool } from './parent';
import { joinPool, PoolConnection, ParallelMethod, ParallelMethods } from './worker';
import { Communicator } from './mpi';
export { createPool, joinPool, WorkerPool, PoolConnection, Communicator, ParallelMethod, ParallelMethods, };
